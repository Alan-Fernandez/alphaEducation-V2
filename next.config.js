/** @type {import('next').NextConfig} */
const nextConfig = {
  // strictPostcssConfiguration: false,
    images: {
        domains: ['backend.alphaeduc.tec.br','10.12.0.101'],
    },
};

module.exports = nextConfig;